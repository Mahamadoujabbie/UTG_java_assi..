
// I created  a package called Question_2 so that question 1 and the question 2 won't be together
package Question_2;

// Network status that are constants(final) in enum class
public enum NetworkStatus {
    CONNECTED,
    DISCONNECTED,
    CONNECTING,
    LIMITED
}
